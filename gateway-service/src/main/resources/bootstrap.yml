# bootstap 的内容是不能被本地的application所覆盖
server:
  port: 5678

spring:
  application:
    name: gateway-service
  cloud:
    config:
      label: master
      profile: dev
      uri: http://localhost:5200/